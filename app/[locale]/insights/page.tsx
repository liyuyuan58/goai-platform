import { SiteHeader } from "@/components/layout/site-header";import { SiteFooter } from "@/components/layout/site-footer";import { marketingArticles } from "@/lib/content-marketing";import type { Locale } from "@/lib/i18n";import Link from "next/link";
export default async function Insights({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <><SiteHeader locale={locale}/><main><section className="border-b border-border py-20"><div className="container-page"><p className="text-xs font-semibold uppercase tracking-[.14em] text-brand">GoAI Insights</p><h1 className="mt-4 text-4xl font-semibold sm:text-6xl">AI execution and global growth</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-secondary">Practical frameworks for global growth, AI tools, agents and forward deployed AI.</p></div></section><section className="bg-surface/45 py-16"><div className="container-page grid gap-5">{marketingArticles.map(a=><article className="rounded-3xl border border-border bg-white p-6" key={a.slug}><p className="text-xs font-semibold uppercase tracking-[.12em] text-brand">{a.pillar}</p><h2 className="mt-3 text-2xl font-semibold">{a.title}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-secondary">{a.description}</p><Link className="mt-5 inline-flex text-sm font-semibold text-brand" href={`/${locale}/insights/${a.slug}`}>Read guide →</Link></article>)}</div></section></main><SiteFooter locale={locale}/></>}
